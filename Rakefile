#                                                                 -*- ruby -*-

require 'rubygems'

version = '>= 0'
gem 'jekyll', version

task :default => :jekyll

task :jekyll do |t|
  load Gem.bin_path('jekyll', 'jekyll', version)
end
