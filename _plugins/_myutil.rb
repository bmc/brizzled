# NOTE: These files are processed, by Jekyll, in alphabetical order. Thus,
# modules must be included by classes that occur in files seen *later*.
# For that reason, all modules are in "_" files.

module Jekyll

  module MyUtil

    private

  end

end
